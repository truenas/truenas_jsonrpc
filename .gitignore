# Cargo build outputs. `Cargo.lock` IS committed (the workspace builds binaries/tests
# and CI runs `--locked` for reproducibility; private consumers clone this tree).
target/

# Ad-hoc performance + profiling scripts — useful locally, not worth carrying in the repo.
/bench/

# Editor swap/backup files
*.swp
*.swo
*~

# OS cruft
.DS_Store
